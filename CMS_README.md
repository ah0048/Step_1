# نظام إدارة المحتوى (CMS) - Step 1

## نظرة عامة

تم تطوير نظام إدارة محتوى شامل يسمح للادمن بإدارة المدربين والمنتجات بسهولة من خلال لوحة تحكم متطورة. النظام يدعم رفع الصور وإدارة المحتوى بشكل ديناميكي.

## الميزات الجديدة

### 1. إدارة المدربين
- ✅ إضافة مدربين جدد مع الصور
- ✅ تعديل بيانات المدربين
- ✅ حذف المدربين (soft delete)
- ✅ رفع صور المدربين
- ✅ إدارة التخصصات والعلامات

### 2. إدارة المنتجات
- ✅ إضافة منتجات جديدة مع الصور
- ✅ تعديل المنتجات
- ✅ حذف المنتجات (soft delete)
- ✅ رفع صور المنتجات
- ✅ إدارة الأسعار والشارات

### 3. نظام رفع الصور
- ✅ دعم أنواع الصور: JPG, PNG, GIF, WebP
- ✅ حد أقصى لحجم الملف: 5MB
- ✅ أسماء ملفات فريدة
- ✅ تخزين آمن في مجلد uploads

## كيفية الاستخدام

### 1. الوصول إلى لوحة التحكم

1. افتح الموقع الرئيسي
2. اضغط على "لوحة التحكم" في الفوتر
3. سجل الدخول باستخدام:
   - **اسم المستخدم:** admin
   - **كلمة المرور:** admin123

### 2. إضافة مدرب جديد

1. في لوحة التحكم، اضغط على "المدربين"
2. اضغط على "إضافة مدرب جديد"
3. املأ البيانات المطلوبة:
   - الاسم
   - التخصص
   - الوصف
   - السعر
   - التقييم
   - العلامات (مفصولة بفواصل)
   - صورة المدرب
4. اضغط "حفظ"

### 3. إضافة منتج جديد

1. في لوحة التحكم، اضغط على "المنتجات"
2. اضغط على "إضافة منتج جديد"
3. املأ البيانات المطلوبة:
   - العنوان
   - الوصف
   - السعر
   - السعر القديم (اختياري)
   - الشارة (اختياري)
   - لون الشارة
   - صورة المنتج
4. اضغط "حفظ"

### 4. تعديل أو حذف العناصر

- **التعديل:** اضغط على أيقونة التعديل (✏️) بجانب العنصر
- **الحذف:** اضغط على أيقونة الحذف (🗑️) بجانب العنصر

## البنية التقنية

### قاعدة البيانات

تم إضافة جدول جديد للمنتجات:

```sql
CREATE TABLE products (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  title TEXT NOT NULL,
  description TEXT NOT NULL,
  price REAL NOT NULL,
  old_price REAL,
  image_url TEXT,
  badge TEXT,
  badge_class TEXT DEFAULT 'red',
  is_active BOOLEAN DEFAULT 1,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### API Endpoints

#### المدربين
- `GET /api/counselors` - الحصول على جميع المدربين
- `GET /api/counselors/:id` - الحصول على مدرب واحد
- `POST /api/counselors` - إضافة مدرب جديد (مدير فقط)
- `PUT /api/counselors/:id` - تحديث مدرب (مدير فقط)
- `DELETE /api/counselors/:id` - حذف مدرب (مدير فقط)

#### المنتجات
- `GET /api/products` - الحصول على جميع المنتجات
- `GET /api/products/:id` - الحصول على منتج واحد
- `POST /api/products` - إضافة منتج جديد (مدير فقط)
- `PUT /api/products/:id` - تحديث منتج (مدير فقط)
- `DELETE /api/products/:id` - حذف منتج (مدير فقط)

### نظام رفع الصور

- **المجلد:** `./uploads`
- **الأنواع المدعومة:** JPG, PNG, GIF, WebP
- **الحد الأقصى:** 5MB
- **الأمان:** أسماء ملفات فريدة مع timestamp

## الملفات المحدثة

### Backend
- `backend/database.js` - إضافة جدول المنتجات
- `backend/routes/products.js` - API للمنتجات
- `backend/server.js` - إضافة مسار المنتجات
- `backend/routes/counselors.js` - تحديث لدعم رفع الصور

### Frontend
- `backend/admin/index.html` - إضافة قسم المنتجات
- `backend/admin/admin.js` - دوال إدارة المنتجات
- `backend/admin/admin.css` - أنماط المنتجات
- `main.js` - تحديث لتحميل البيانات من API

## الأمان

- ✅ مصادقة المدير مطلوبة لجميع عمليات التعديل
- ✅ تحقق من أنواع الملفات المرفوعة
- ✅ حد أقصى لحجم الملفات
- ✅ أسماء ملفات فريدة لمنع التضارب

## التوافق

النظام متوافق مع:
- ✅ المتصفحات الحديثة
- ✅ الأجهزة المحمولة
- ✅ البيانات الثابتة كبديل عند عدم توفر API

## استكشاف الأخطاء

### مشاكل شائعة

1. **لا تظهر الصور:**
   - تأكد من تشغيل الخادم
   - تحقق من مسار مجلد uploads

2. **خطأ في رفع الصور:**
   - تأكد من أن الملف أقل من 5MB
   - تأكد من أن نوع الملف مدعوم

3. **لا يعمل API:**
   - تأكد من تشغيل الخادم على المنفذ 3001
   - تحقق من اتصال قاعدة البيانات

## الدعم

للحصول على الدعم أو الإبلاغ عن مشاكل، يرجى التواصل مع فريق التطوير.

---

**تم التطوير بواسطة:** فريق Step 1  
**التاريخ:** ديسمبر 2024  
**الإصدار:** 1.0.0






